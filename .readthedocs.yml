version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  apt_packages:
    - libldap2-dev
    - libsasl2-dev

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
